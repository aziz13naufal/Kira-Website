export const metadata = {
    title: 'Life Note'
}

const LifeNoteLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>{children}</div>
  )
}

export default LifeNoteLayout
